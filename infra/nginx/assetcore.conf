# AssetCore — nginx reverse proxy (production)

upstream assetcore_backend {
  server backend:4000;
}

server {
  listen 80;
  server_name _;

  client_max_body_size 12m;

  # Redirect HTTP to HTTPS when TLS certs are mounted (uncomment after certbot)
  # return 301 https://$host$request_uri;

  location /api/ {
    proxy_pass http://assetcore_backend;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;
  }

  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
}

# HTTPS server — enable after obtaining certificates
# server {
#   listen 443 ssl http2;
#   server_name assetcore.example.com;
#
#   ssl_certificate     /etc/letsencrypt/live/assetcore.example.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/assetcore.example.com/privkey.pem;
#
#   add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#   client_max_body_size 12m;
#
#   location /api/ {
#     proxy_pass http://assetcore_backend;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#
#   location / {
#     root /usr/share/nginx/html;
#     try_files $uri $uri/ /index.html;
#   }
# }
